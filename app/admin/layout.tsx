import { ReactNode } from "react";

export const metadata = {
  title: "LegalCals Authority Admin",
};

export default function AdminLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
